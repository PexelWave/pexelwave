import React from 'react'

type Props = {}

const HLine = (props: Props) => {
  return (
    <div className="h-[2px] w-[50px] md:w-[200px] bg-blue rounded-full animate-bounce"></div>
  )
}

export default HLine