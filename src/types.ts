export type CardType = {
    icon?: React.ReactNode
    title: string;
    description: string;
    href: string
}

export type NavLink = {
    icon?: React.ReactNode;
    text: string;
    href: string;
}